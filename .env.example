# Example environment variables for Convex + Svelte application
# Copy this file to .env.local and fill in your values

# ============================================================================
# CONVEX DEPLOYMENT (Auto-generated by `npx convex dev`)
# ============================================================================
CONVEX_DEPLOYMENT=dev:your-deployment-name
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# ============================================================================
# AUTHENTICATION SETUP (Set in Convex Dashboard or via CLI)
# ============================================================================

# Google OAuth Credentials (from Google Cloud Console)
# Required for Google sign-in to work
# Get these from: https://console.cloud.google.com/apis/credentials
# AUTH_GOOGLE_ID=your-client-id.apps.googleusercontent.com
# AUTH_GOOGLE_SECRET=GOCSPX-your-client-secret

# Your application URL (used for OAuth redirects)
# Development: http://localhost:5174
# Production: https://your-domain.com
# SITE_URL=http://localhost:5174

# JWT Keys (auto-generated by Convex Auth on first sign-in)
# JWKS={"keys":[...]}
# JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----...

# ============================================================================
# EMAIL SETUP (Required for password reset functionality)
# ============================================================================

# Resend API Key (from https://resend.com/api-keys)
# Required for sending password reset emails
# RESEND_API_KEY=re_your_api_key_here

# From Email Address (must be verified in Resend)
# This should be a domain you own and have verified in Resend
# FROM_EMAIL=noreply@yourdomain.com

# Application Name (used in email templates)
# APP_NAME=Room Dates

# ============================================================================
# PAYMENT SETUP (Required for credit purchases)
# ============================================================================

# Stripe API Keys (from https://dashboard.stripe.com/apikeys)
# Use test keys for development, live keys for production
# STRIPE_PUBLISHABLE_KEY=pk_test_your_test_key_here
# STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Public Stripe Key for client-side (add to .env.local, not Convex)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_test_key_here

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Run `npx convex dev` to generate CONVEX_* variables
# 2. For Google OAuth, see docs/AUTHENTICATION_SETUP.md
# 3. For email setup, see docs/EMAIL_SETUP.md (if exists)
# 4. For Stripe payments, get keys from https://dashboard.stripe.com/apikeys
# 5. Set AUTH_*, RESEND_*, STRIPE_* variables via:
#    `npx convex env set VARIABLE_NAME value`
# 6. JWT keys are auto-generated on first authentication attempt

# ============================================================================
# SECURITY NOTES
# ============================================================================
# - Never commit actual .env files to version control
# - The .gitignore file excludes .env.* files except this example
# - Keep your Google OAuth client secret and Resend API key secure
# - Use different credentials for development vs production
# - Only use verified domains for FROM_EMAIL in production

