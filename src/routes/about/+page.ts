// Enable prerendering for this static page
// This page will be generated at build time and served from CDN
export const prerender = true;

// Disable SSR since this is a static page
export const ssr = true;

// Enable client-side routing for better UX
export const csr = true;
